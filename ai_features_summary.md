# 🌊 水井村 AI 導覽與智慧水質監控系統 — 功能彙整與健檢報告

本文件詳細統整目前已開發的 **AI 水井龜導覽助理** 系統功能，並說明其核心邏輯（權限過濾、閾值判定、健檢流程、多元呈現），以便漁民與系統管理員快速了解與維護。

---

## 📌 一、 UI 介面優化與使用者體驗

1. **懸浮對話視窗 (Floating Mascot Chat) 頭像放大**
   - **調整點**：[mascot.css](file:///d:/workspace/shuijing_web/static/css/core/mascot.css) 中的 `.mascot-chat-avatar`。
   - **修改後**：容器外框從 `42px` 放大至 `52px`，圓角設為 `14px`；內部的 `sprite.png` 像素烏龜大小從 `38px` 提升至 `48px`。
   - **效果**：標題列的像素烏龜更為醒目、精緻，提升親和力。

2. **完整對話頁面 (Full-Page Chat) 頭像放大**
   - **調整點**：[chat.html](file:///d:/workspace/shuijing_web/chat/templates/chat/chat.html) 中的 `.chat-page-avatar`。
   - **修改後**：容器外框從 `46px` 放大至 `56px`，圓角設為 `12px`；內部的像素烏龜大小從 `42px` 提升至 `50px`。

3. **語音錄音與辨識等待動畫優化**
   - **調整點**：[mascot_chat.js](file:///d:/workspace/shuijing_web/static/js/core/mascot_chat.js)、[chat.html](file:///d:/workspace/shuijing_web/chat/templates/chat/chat.html) 與 [mascot.css](file:///d:/workspace/shuijing_web/static/css/core/mascot.css) 的語音與對話模組。
   - **倒數計時**：點擊麥克風後，輸入框提示會變更為 `🎙️ 聆聽中... 還可以說 X 秒`。
   - **旋轉載入圖示**：傳送至 ASR 伺服器進行辨識時，麥克風按鈕圖示會自動轉換為**旋轉箭頭 (`bi-arrow-clockwise mascot-spin`)**，帶有平滑的 360 度旋轉動畫。
   - **動態點點點提示**：等待辨識期間，輸入框佔位文字會以每 400ms 動態增加點號：`⚡ 正在辨識中...` 循環。
   - **像素烏龜思考氣泡動畫 (Mascot Typing Loader)**：當 AI 正在計算時，對話氣泡內會出現迷你像素烏龜，踏步揮手。
   - **互動式大烏龜等待面板 (Mascot Waiting Overlay)**：當語音辨識進行中，或 AI 正在後台遠程撈取大數據時，對話日誌上方會升起一個**半透明毛玻璃特效面板 (`.mascot-waiting-overlay`)**，卡片中央會展現一隻**大尺寸的像素烏龜 (76px * 76px)**。它會一邊快樂地上下彈跳 (`mascot-waiting-bounce`)，一邊旋轉循環切換牠的 4 個行走幀動畫 (`mascot-waiting-dance`)，就像在池邊手舞足蹈，非常滑稽可愛。
   - **語境適應提示**：等待卡片會根據使用者提問內容，顯示擬人化的動態說明（如：`🐢 水井龜正在潛水中撈取水質數據...`、`🐢 水井龜正在佈告欄前查看最新公告...`、`🐢 水井龜正在為您計算池子穩定度評分...`）。
   - **效果**：在幾秒鐘的後端連線/AI 推理等待期，提供極佳的多媒體視覺包裝與趣味性，徹底避免死板乾等，創造驚豔的互動體驗。

4. **開啟對話視窗時自動置底**
   - **調整點**：[mascot_chat.js](file:///d:/workspace/shuijing_web/static/js/core/mascot_chat.js) 中的 `openPanel()` 流程。
   - **修改後**：點擊懸浮烏龜開啟聊天面板時，立即執行 `scrollLog()` 強制對話歷史滾動至最底部，並在 80 毫秒輸入框聚焦完成後再次校準置底。
   - **效果**：解決了因對話框隱藏時無法精確計算高度，導致開啟後畫面停留在舊訊息、需要手動往下拉才能看到最新數據或推薦快捷按鈕的困擾。

---

## 🔒 二、 安全機制與權限健檢

系統在所有 AI 水質查詢功能中，皆強制執行**使用者帳號驗證與魚池權限過濾**：

1. **訪客阻斷**
   - 若使用者未登入（訪客），AI 會禮貌拒絕並提示「請先登入帳號以查看您的魚池數據」，避免外部未授權人員探查池區隱私。
2. **權限限縮 (`visible_ponds_for`)**
   - 所有的水質查詢工具在底層皆會呼叫 `visible_ponds_for(user)`，僅撈取該登入帳號具有檢視權限的魚池。
   - 若使用者嘗試詢問不屬於他的池名（例如輸入非本人的魚池名稱），系統會回傳：「找不到此魚池或權限不足」，並列出使用者實際擁有權限的魚池列表。

---

## 🎯 三、 閾值（閥值）判定與自訂邏輯

系統判斷水質數值（溶氧量、水溫、pH 等）是否正常，絕非寫死（Hardcoded）在程式中，而是**動態撈取閾值**：

1. **預設閾值**
   - 系統若未設定自訂閾值，會自動採用 `water/utils.py` 中的預設閾值：
     - **溶氧量 (DO)**：`4.5` ~ `8.5` mg/L
     - **水溫 (Temp)**：`20` ~ `30` °C
     - **pH 值**：`7.5` ~ `8.5`
     - **氨氮**：`0` ~ `0.2` mg/L
     - **亞硝酸鹽**：`0` ~ `0.15` mg/L
2. **自訂閾值 (`WaterThreshold`)**
   - 若漁民於管理後台或設定頁面針對特定池區、特定感測器設定了 `WaterThreshold`，AI 呼叫工具時會**優先載入該使用者的自訂閥值**進行比對與健康判定。

---

## 🛠️ 四、 AI 水質監控功能健檢 (7 大 Tool 詳解)

AI 助理後端綁定了 7 個核心 Tools，皆已完成註冊與功能健檢。以下為各功能的運作機制與適用場景：

### 1. `get_pond_summary` (水池狀態統整)
- **漁民常用問法**：「現在各個池子狀況如何？」、「列出全部水質」
- **健檢項目**：
  - 檢視所有具權限的魚池。
  - 撈取最新一筆感測數據，並計算近 N 天（預設 7 天）的平均值與溶氧極值。
  - **支援多感測器**：傳回資料包含 `sensors` 列表，包含池內各個活躍感測器的最新量測值，使 AI 能在統整時列出個別感測器狀況。
  - **顯示方式**：AI 會在回答中附帶 `[DASHBOARD]` 標記區塊，前端會直接將其渲染成**精美數據看板卡片**。

### 2. `get_latest_water_quality` (最新水質速查)
- **漁民常用問法**：「文蛤一號池水溫多少？」、「我的魚池現在水質還好嗎？」
- **健檢項目**：
  - 驗證該池是否在權限內。
  - 取得該池最新的一筆測量快照（水溫、pH、溶氧、鹽度）。
  - **支援多感測器**：特別回傳 `sensors` 列表，如果池中裝有複數感測器，會一併列出各個感測器的個別最新數值，避免單一平均值掩蓋局部異常。
  - **顯示方式**：配合 Emojis 呈現清單，並用表格化呈現最新數據。

### 3. `get_average_do` (溶氧平均值統計)
- **漁民常用問法**：「文蛤一號池這幾天的平均溶氧是多少？」
- **健檢項目**：
  - 計算近 N 天的溶氧平均，提供漁民評估長期水質穩定度。

### 4. `get_pond_history` (歷史趨勢查詢)
- **漁民常用問法**：「幫我畫出文蛤一號池近七天的溶氧趨勢圖」
- **健檢項目**：
  - 抓取最近 7 到 30 天的歷史數據，並等距抽樣出 15 筆點位。
  - **顯示方式**：AI 會在回答中附帶 `[CHART]` 區塊。前端收到後會以 **`Chart.js` 渲染出互動折線圖/長條圖**，漁民可將滑鼠懸停看精確數值。

### 5. `get_active_alerts` (最新警報檢查)
- **漁民常用問法**：「現在有哪些警報？」、「有沒有正在缺氧的池子？」
- **健檢項目**：
  - 檢查資料庫中 `WaterSensorAlert` 且 `is_active=True` 的項目。
  - 過濾出屬於該漁民可見池區的警報。
  - **顯示方式**：AI 以 Markdown 表格列出所有處於警報中的感測器，標示 🚨 警報與觸發時間。

### 6. `get_pond_stability_score` (水質穩定度健檢)
- **漁民常用問法**：「我的文蛤一號池穩定度好不好？」、「分析一下穩定度評分」
- **健檢項目**：
  - 抓取該池最新讀值與該使用者的 `WaterThreshold`。
  - 讀取該池專屬的溶氧、pH、水溫計算權重（例如 `stability_w_do`）。
  - 依偏離比例計算出 0 ~ 100% 的**綜合穩定度評分**。
  - **顯示方式**：以「🛡️ **水質穩定度健檢：XX%**」呈現，並以表格列出各指標的數值、閾值區間與權重。

### 7. `get_pond_sensors_readings` (感測器逐一健檢)
- **漁民常用問法**：「檢查文蛤一號池的所有感測器都有看了嗎？」、「列出所有感測器狀態」
- **健檢項目**：
  - **逐一檢查**：遍歷該池底下所有啟用中 (`is_active=True`) 的感測器（例如 A 區、B 區感測器），而非只撈取整池的單一平均。
  - **閾值對比**：針對每個感測器，個別讀取專屬閾值，逐一比對溫度、pH、溶氧、氨氮、亞硝酸鹽、鹽度，判定其狀態（正常、偏高、偏低）。
  - **漏看檢驗**：若感測器無讀值，標記為 `尚無量測數據`。
  - **顯示方式**：使用 Markdown 橫向表格，一目了然列出所有感測器的名稱、狀態與細項數值，確保漁民不會遺漏任何監控死角。

---

## 🎨 五、 互動回覆呈現方式

為了給予漁民最佳的視覺感受，AI 助理的回覆格式已全面優化：

1. **圖表渲染 (Charts)**：折線圖 (Line)、長條圖 (Bar) 均採用 Chart.js 動態繪製，帶有微動畫與懸停工具提示。
2. **精美看板 (Dashboard Cards)**：統整數據時直接以精緻的主題卡片呈現最新數值。
3. **豐富表格 (Markdown Tables)**：在警報列表與感測器逐一檢查時，用整齊的表格對齊數值，方便手機與電腦閱讀。
4. **Emoji 情境標示**：
   - 🛡️ 代表穩定度健檢
   - 🚨 代表異常警報
   - 🟢 代表數值正常良好
   - ⚠️ 代表警告需留意
   - ❓ 代表無數據或尚未設定

---

## 💡 六、 漁民快速測試指令 (UI 快速按鈕)

我們已將上述最常用、最核心的漁民健檢功能，做成對話視窗下方的**快捷按鈕**，點擊即可一鍵測試：
1. 🌾 **水池統整**：`請整理我所有魚池的水質狀態與最新讀值，並分析有沒有異常？`
2. 🏥 **魚池完整健檢**：`請幫我對文蛤一號池進行完整的功能健檢，包含計算穩定度與逐一感測器讀值檢查。`
3. 🔔 **最新警報檢查**：`請檢查目前有哪些魚池或感測器處於異常警報狀態？`
4. 📊 **水質趨勢圖表**：`請用折線圖畫出文蛤一號池的最新水質歷史趨勢圖`
5. 📅 **最新活動** & 📢 **網站公告**
